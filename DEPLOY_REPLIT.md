# 🚀 วิธี Deploy บน Replit

## ⚠️ ข้อจำกัดสำคัญ

**Replit Free Tier จะ Sleep:**
- Bot จะ sleep หลังไม่ใช้งาน **5 นาที**
- เมื่อ sleep: Bot จะไม่ตอบสนองทันที ต้องรอ wake up
- **ต้อง upgrade เป็น Replit Core ($7/เดือน)** เพื่อรันตลอดเวลา

---

## ขั้นตอนที่ 1: สร้าง Repl ใหม่

1. ไปที่ **https://replit.com**
2. Login หรือสมัครสมาชิก
3. คลิก **+ Create Repl**
4. เลือก:
   - **Template:** Node.js
   - **Title:** `LoLBuildBot` (หรือชื่อที่ต้องการ)
5. คลิก **Create Repl**

---

## ขั้นตอนที่ 2: Import Code

### วิธีที่ 1: Import จาก GitHub (แนะนำ)

1. ใน Replit → คลิก **Version Control** (Git icon)
2. คลิก **Import from GitHub**
3. ใส่ URL ของ repository: `https://github.com/your-username/LoLBuildBot`
4. คลิก **Import**

### วิธีที่ 2: Upload Files

1. Upload ไฟล์ทั้งหมดไปยัง Replit
2. หรือใช้ **Git** ใน Replit:
   ```bash
   git clone https://github.com/your-username/LoLBuildBot.git
   ```

---

## ขั้นตอนที่ 3: ติดตั้ง Dependencies

1. เปิด **Shell** ใน Replit
2. รันคำสั่ง:
   ```bash
   npm install
   ```

---

## ขั้นตอนที่ 4: ตั้งค่า Environment Variables (Secrets)

1. ใน Replit → คลิก **Secrets** (🔒 icon) หรือ **Tools** → **Secrets**
2. เพิ่ม Secrets:
   - **Key:** `TOKEN`
   - **Value:** Discord bot token ของคุณ
   - คลิก **Add secret**
   
   - **Key:** `DEV_GUILD_ID` (optional)
   - **Value:** Guild ID สำหรับ dev commands
   - คลิก **Add secret**

**หมายเหตุ:** Replit ใช้ **Secrets** แทน `.env` file

---

## ขั้นตอนที่ 5: แก้ไข Code สำหรับ Replit

Replit ใช้ `process.env` โดยตรง ไม่ต้องใช้ dotenv:

1. ตรวจสอบว่า `src/index.ts` โหลด environment variables ถูกต้อง
2. Keep-Alive server จะใช้ PORT จาก Replit อัตโนมัติ

---

## ขั้นตอนที่ 6: รัน Bot

1. คลิก **Run** button (▶️)
2. ตรวจสอบ **Console** ว่า bot รันสำเร็จ
3. ควรเห็น:
   ```
   [KeepAlive] ✅ Server running on port 8080
   [KeepAlive] 📡 Ready for UptimeRobot pings!
   Ready! Logged in as YourBot#1234
   ```

---

## ขั้นตอนที่ 7: ตั้งค่า UptimeRobot

1. ดู **Webview** URL ใน Replit (เช่น: `https://your-repl-name.your-username.repl.co`)
2. ตั้งค่า UptimeRobot ตาม [UPTIMEROBOT_SETUP.md](./UPTIMEROBOT_SETUP.md)
3. ใช้ URL จาก Replit Webview

---

## ⚠️ เรื่อง Sleep (สำคัญ!)

### Replit Free Tier:
- ❌ **จะ sleep หลังไม่ใช้งาน 5 นาที**
- ❌ Bot จะไม่ตอบสนองทันที
- ✅ UptimeRobot ช่วยลดโอกาส sleep แต่**ไม่ guarantee**

### Replit Core ($7/เดือน):
- ✅ **รันตลอดเวลา ไม่ sleep**
- ✅ เหมาะกับ Discord bot

---

## 🔧 Troubleshooting

### Bot ไม่รัน
- ตรวจสอบ Console logs
- ตรวจสอบว่า TOKEN ถูกต้อง (ใน Secrets)
- ตรวจสอบว่า dependencies ติดตั้งครบ

### Bot Sleep
- นี่เป็นพฤติกรรมปกติของ Replit free tier
- ถ้าต้องการรันตลอดเวลา: ต้อง upgrade เป็น Replit Core

### Keep-Alive Server ไม่ทำงาน
- ตรวจสอบ Console logs
- ตรวจสอบว่า PORT ถูกต้อง
- Replit จะกำหนด PORT อัตโนมัติ

### Canvas ไม่ทำงาน
- Replit อาจต้องติดตั้ง system dependencies
- ตรวจสอบ Console logs สำหรับ errors

---

## 💡 Tips

1. **Always On:** ถ้า upgrade เป็น Replit Core จะมี Always On feature
2. **Webview:** ใช้ Webview URL สำหรับ UptimeRobot
3. **Secrets:** ใช้ Secrets แทน `.env` file
4. **Auto-save:** Replit auto-save code อัตโนมัติ

---

## ✅ Checklist

- [ ] สร้าง Repl ใหม่
- [ ] Import code จาก GitHub
- [ ] ติดตั้ง dependencies (`npm install`)
- [ ] เพิ่ม Secrets (TOKEN, DEV_GUILD_ID)
- [ ] รัน bot
- [ ] ตรวจสอบ Console logs
- [ ] ตั้งค่า UptimeRobot (ถ้าต้องการ)
- [ ] ทดสอบ bot ใน Discord

---

## 📞 Support

- **Replit Docs:** https://docs.replit.com
- **Replit Discord:** https://discord.gg/replit
- **Replit Status:** https://status.replit.com

---

## 💰 Pricing

- **Free:** Sleep หลังไม่ใช้งาน 5 นาที
- **Core ($7/เดือน):** รันตลอดเวลา ไม่ sleep

---

**พร้อมแล้ว! Bot ของคุณควรจะรันบน Replit แล้ว** 🎉

**หมายเหตุ:** Replit free tier จะ sleep - ถ้าต้องการรันตลอดเวลา ต้อง upgrade เป็น Replit Core

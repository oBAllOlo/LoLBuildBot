# 🚀 วิธี Deploy บน Render

## ⚠️ ข้อจำกัดสำคัญ

**Render Free Tier จะ Sleep:**
- Bot จะ sleep หลังไม่ใช้งาน **15 นาที**
- เมื่อ sleep: Bot จะไม่ตอบสนองทันที ต้องรอ wake up (30-60 วินาที)
- ถ้ามีคนใช้บ่อย: อาจไม่ sleep บ่อย

---

## ขั้นตอนที่ 1: เตรียม Bot

### 1.1 สร้างไฟล์ `.env` (สำหรับ local testing)
```env
TOKEN=your_discord_bot_token_here
DEV_GUILD_ID=your_guild_id_here  # optional
```

### 1.2 Push ขึ้น GitHub
```bash
git add .
git commit -m "Prepare for Render deployment"
git push origin main
```

---

## ขั้นตอนที่ 2: สมัคร Render

1. ไปที่ **https://render.com**
2. คลิก **Get Started for Free**
3. สมัครด้วย **GitHub** (แนะนำ) หรือ Email
4. Verify email (ถ้ามี)

---

## ขั้นตอนที่ 3: สร้าง Web Service

1. Login เข้า Render dashboard
2. คลิก **New +** → **Web Service**
3. เชื่อมต่อ GitHub repository:
   - คลิก **Connect GitHub**
   - เลือก repository: `LoLBuildBot`
   - คลิก **Connect**

---

## ขั้นตอนที่ 4: ตั้งค่า Service

### Basic Settings:
- **Name:** `lolbuildbot` (หรือชื่อที่ต้องการ)
- **Region:** เลือกที่ใกล้ที่สุด (Singapore, Frankfurt, etc.)
- **Branch:** `main` หรือ `master`
- **Root Directory:** `/` (root)
- **Runtime:** `Node`
- **Build Command:** `npm install`
- **Start Command:** `npm start`
- **Plan:** `Free`

### Advanced Settings (Optional):
- **Auto-Deploy:** `Yes` (deploy อัตโนมัติเมื่อ push code)

---

## ขั้นตอนที่ 5: ตั้งค่า Environment Variables

1. ใน Render dashboard → เลือก service ของคุณ
2. ไปที่ **Environment** tab
3. เพิ่ม Environment Variables:
   - **Key:** `TOKEN`
   - **Value:** Discord bot token ของคุณ
   - คลิก **Save Changes**
   
   - **Key:** `DEV_GUILD_ID` (optional)
   - **Value:** Guild ID สำหรับ dev commands
   - คลิก **Save Changes**

---

## ขั้นตอนที่ 6: Deploy

1. คลิก **Create Web Service**
2. Render จะเริ่ม build และ deploy อัตโนมัติ
3. รอให้ build เสร็จ (ประมาณ 2-5 นาที)
4. ตรวจสอบ **Logs** ว่า bot รันสำเร็จ

---

## ขั้นตอนที่ 7: ตรวจสอบ Bot

1. ดู **Logs** ใน Render dashboard
2. ตรวจสอบว่า bot online ใน Discord
3. ทดสอบ command: `/ping` หรือ `/build Yasuo`

---

## ⚠️ หมายเหตุสำคัญ

### เรื่อง Sleep
- Render free tier จะ sleep หลังไม่ใช้งาน 15 นาที
- ถ้ามีคนใช้บ่อย: อาจไม่ sleep
- ถ้า sleep: ผู้ใช้ต้องรอ 30-60 วินาที เพื่อ wake up

### Keep-Alive (Optional)
ถ้าต้องการป้องกัน sleep (แต่ไม่ guarantee):
- ใช้ external service เช่น UptimeRobot เพื่อ ping bot ทุก 10 นาที
- หรือใช้ cron job เพื่อ wake up bot

### Canvas Dependencies
Render ควรจะติดตั้ง `@napi-rs/canvas` อัตโนมัติ แต่ถ้ามีปัญหา:
- ตรวจสอบ Logs
- อาจต้องเพิ่ม build command: `npm install --include=dev`

---

## 🔧 Troubleshooting

### Bot ไม่รัน
- ตรวจสอบ Logs ใน Render dashboard
- ตรวจสอบว่า TOKEN ถูกต้อง
- ตรวจสอบว่า dependencies ติดตั้งครบ

### Bot Sleep
- นี่เป็นพฤติกรรมปกติของ Render free tier
- ถ้าต้องการรันตลอดเวลา: ต้อง upgrade เป็น paid plan

### Canvas ไม่ทำงาน
- ตรวจสอบ Logs
- Render ควรจะ support canvas อัตโนมัติ

### Build Failed
- ตรวจสอบ Logs
- ตรวจสอบว่า package.json ถูกต้อง
- ตรวจสอบว่า Node.js version ถูกต้อง

---

## 📞 Support

- **Render Docs:** https://render.com/docs
- **Render Status:** https://status.render.com
- **Discord Support:** ดูใน Render dashboard

---

## ✅ Checklist

- [ ] Push code ขึ้น GitHub
- [ ] สมัคร Render
- [ ] สร้าง Web Service
- [ ] ตั้งค่า Build/Start commands
- [ ] เพิ่ม Environment Variables (TOKEN)
- [ ] Deploy
- [ ] ตรวจสอบ Logs
- [ ] ทดสอบ bot ใน Discord

---

## 💡 Tips

1. **Auto-Deploy:** เปิดไว้เพื่อ deploy อัตโนมัติเมื่อ push code
2. **Logs:** ตรวจสอบ Logs เป็นประจำเพื่อดู errors
3. **Sleep:** จำไว้ว่า free tier จะ sleep ถ้าไม่ใช้งาน

---

**พร้อมแล้ว! Bot ของคุณควรจะรันบน Render แล้ว** 🎉

**หมายเหตุ:** Bot อาจจะ sleep ถ้าไม่ใช้งาน 15 นาที - นี่เป็นข้อจำกัดของ Render free tier

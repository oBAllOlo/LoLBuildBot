# Discord Bot Configuration
# สำหรับการทดสอบบน localhost (Development)

# ============================================
# ⚠️ สำคัญ: สำหรับ Local Testing
# ============================================
# 1. Copy ไฟล์นี้เป็น .env: copy env.template .env
# 2. แก้ไขค่าต่างๆ ใน .env (ไฟล์ .env จะไม่ถูก commit)

# ============================================
# Bot Tokens
# ============================================
# คุณสามารถใช้ TOKEN เดียว หรือแยก TOKEN_PROD และ TOKEN_TEST

# วิธีที่ 1: ใช้ TOKEN เดียว (จะใช้ token นี้เสมอ)
TOKEN=

# วิธีที่ 2: แยก token สำหรับ prod และ test (แนะนำ)
# TOKEN_PROD จะใช้เมื่อ NODE_ENV=production
# TOKEN_TEST จะใช้เมื่อ NODE_ENV=development
TOKEN_PROD=your_production_bot_token_here
TOKEN_TEST=your_test_bot_token_here

# ============================================
# Development Settings
# ============================================
# ตั้งค่า DEV_GUILD_IDS
# - ใส่ Guild ID ของ test server ของคุณ
# - หา Guild ID: เปิด Developer Mode ใน Discord → Right-click server → Copy ID
# - สามารถใส่หลาย ID ได้โดยคั่นด้วย comma: id1,id2,id3
DEV_GUILD_IDS=

# Environment (development สำหรับ localhost, production สำหรับ Render)
NODE_ENV=development

# Port สำหรับ Keep-Alive server (optional, default: 8080)
PORT=8080

# ============================================
# วิธีใช้งาน:
# ============================================
# Localhost (Development):
#   - ตั้ง NODE_ENV=development
#   - ใช้ TOKEN_TEST หรือ TOKEN
#   - ตั้ง DEV_GUILD_IDS=your_test_server_id
#
# Production (Render):
#   - ตั้ง NODE_ENV=production (หรือไม่ตั้ง)
#   - ใช้ TOKEN_PROD หรือ TOKEN
#   - ไม่ต้องตั้ง DEV_GUILD_IDS (หรือตั้งเป็นค่าว่าง)

# 🤖 วิธีตั้งค่า UptimeRobot สำหรับ Keep-Alive

## วัตถุประสงค์
ใช้ UptimeRobot เพื่อ ping bot ทุก 5 นาที เพื่อป้องกันไม่ให้ bot sleep บน Render free tier

---

## ขั้นตอนที่ 1: ตรวจสอบ Keep-Alive Server

1. Deploy bot บน Render
2. ตรวจสอบว่า bot รันสำเร็จ
3. ดู URL ของ bot ใน Render dashboard (เช่น: `https://lolbuildbot.onrender.com`)

---

## ขั้นตอนที่ 2: ทดสอบ Keep-Alive Endpoint

1. เปิด browser
2. ไปที่ URL: `https://your-bot-url.onrender.com`
3. ควรเห็นข้อความ: **"I'm still running! Bot is alive! 🤖"**

ถ้าเห็นข้อความนี้ = Keep-Alive server ทำงานแล้ว! ✅

---

## ขั้นตอนที่ 3: สมัคร UptimeRobot

1. ไปที่ **https://uptimerobot.com**
2. คลิก **Sign Up** (ฟรี 100%)
3. สมัครด้วย Email หรือ Google
4. Verify email (ถ้ามี)

---

## ขั้นตอนที่ 4: สร้าง Monitor

1. Login เข้า UptimeRobot dashboard
2. คลิก **Add New Monitor** หรือ **+ New Monitor**

### ตั้งค่า Monitor:

**Monitor Type:**
- เลือก **HTTP(s)**

**Friendly Name:**
- `LoLBuildBot Keep-Alive` (หรือชื่อที่ต้องการ)

**URL (or IP):**
- ใส่ URL ของ bot บน Render
- ตัวอย่าง: `https://lolbuildbot.onrender.com`
- หรือ: `https://lolbuildbot.onrender.com:8080` (ถ้าใช้ port 8080)

**Monitoring Interval:**
- เลือก **5 minutes** (แนะนำ)
- หรือ **1 minute** (ถ้าต้องการ ping บ่อยขึ้น)

**Alert Contacts:**
- เลือก email หรือ notification ที่ต้องการ (optional)

**Status Pages:**
- (optional) เลือก status page ถ้ามี

---

## ขั้นตอนที่ 5: Save และเริ่ม Monitor

1. คลิก **Create Monitor**
2. UptimeRobot จะเริ่ม ping bot ทุก 5 นาที
3. ตรวจสอบใน dashboard ว่า monitor ทำงาน (ควรเป็นสีเขียว)

---

## ตรวจสอบผลลัพธ์

### ใน UptimeRobot Dashboard:
- ✅ **Status:** Up (สีเขียว)
- ✅ **Response Time:** แสดงเวลาตอบสนอง
- ✅ **Uptime:** แสดงเปอร์เซ็นต์ uptime

### ใน Render Logs:
- ควรเห็น requests จาก UptimeRobot ทุก 5 นาที
- Logs จะแสดง: `GET /` requests

---

## ⚠️ หมายเหตุสำคัญ

### 1. Render Free Tier
- UptimeRobot **ช่วยลดโอกาส sleep** แต่**ไม่ guarantee 100%**
- Render free tier อาจยัง sleep ได้ถ้า:
  - มี traffic น้อยมาก
  - หรือ Render ตรวจพบว่าเป็น automated ping

### 2. Monitoring Interval
- **5 minutes:** แนะนำ (ไม่ใช้ quota เยอะ)
- **1 minute:** Ping บ่อยขึ้น แต่ใช้ quota มากขึ้น
- UptimeRobot free tier: 50 monitors, unlimited checks

### 3. Port
- Render จะกำหนด PORT อัตโนมัติ
- Keep-alive server จะใช้ PORT จาก environment variable
- ถ้าไม่มี PORT จะใช้ 8080 เป็น default

---

## 🔧 Troubleshooting

### UptimeRobot แสดง "Down"
- ตรวจสอบว่า bot รันอยู่บน Render
- ตรวจสอบ URL ว่าถูกต้อง
- ตรวจสอบ Render logs ว่ามี error หรือไม่

### Bot ยัง sleep อยู่
- UptimeRobot ช่วยลดโอกาส sleep แต่ไม่ guarantee
- ถ้าต้องการรันตลอดเวลา 100%: ต้อง upgrade Render เป็น paid plan

### Keep-Alive Server ไม่ทำงาน
- ตรวจสอบ Render logs
- ตรวจสอบว่า PORT ถูกต้อง
- ตรวจสอบว่า code deploy แล้ว

---

## ✅ Checklist

- [ ] Bot deploy บน Render สำเร็จ
- [ ] ทดสอบ Keep-Alive endpoint (เห็นข้อความ "I'm still running!")
- [ ] สมัคร UptimeRobot
- [ ] สร้าง Monitor
- [ ] ตั้งค่า URL และ Interval
- [ ] Monitor แสดง "Up" (สีเขียว)
- [ ] ตรวจสอบ Render logs ว่ามี requests จาก UptimeRobot

---

## 📞 Support

- **UptimeRobot:** https://uptimerobot.com
- **UptimeRobot Docs:** https://uptimerobot.com/api/
- **Render Docs:** https://render.com/docs

---

**พร้อมแล้ว! Bot ของคุณควรจะไม่ sleep บ่อยแล้ว** 🎉

**หมายเหตุ:** UptimeRobot ช่วยลดโอกาส sleep แต่ไม่ guarantee 100% สำหรับ Render free tier
